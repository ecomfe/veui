import '../icons/search'
import config from 'veui/managers/config'

config.defaults({
  icons: {
    search: 'search'
  },
  ui: {
    size: {
      values: ['large', 'small']
    }
  }
}, 'searchbox')
